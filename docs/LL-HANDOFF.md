# Linen Legends — Session transfer kit

**Written:** 20 September 2026 · **Owner:** Callum Page · **Branch of record:** `claude/laundry-legends-build-qmcafm`
**Read this file plus `docs/laundrylegends-build-plan.md` (§12 first) before touching anything.**

---

## 1. PASTE THIS INTO THE NEW CLAUDE FIRST

> I'm continuing the Linen Legends build (laundry pickup/delivery, Brisbane/Logan/SEQ). Repo `github.com/xrcallum/King-laundry-`, branch `claude/laundry-legends-build-qmcafm`. Read `docs/LL-HANDOFF.md` and `docs/laundrylegends-build-plan.md` §12 before doing anything — they are the state of record and carry the compliance rules that must never be violated.
>
> The site is BUILT: Vite+React at the repo root, 17 routes + 404, serverless API under `api/`, deployed via the Vercel project `linenlegends-site` (team keatonmills) which auto-deploys every push to this branch. Gates live in `verify/` — smoke.mjs, sentinels.mjs, shots.mjs — and every one must print RESULT: PASS before any push. Work milestone-style: batch work, run gates once per batch, commit per batch with the artefact-style messages already in `git log`.
>
> Hard rules: prices come only from `api/_lib/pricing.js`; coverage only from `src/data/coverageData.js`; copy only from `site/laundrylegends-site.html` or the 49-page PDF extract; never invent facts, figures, contacts, reviews or features; keep every compliance sentinel (sentinels.mjs enforces them); brand is "Linen Legends"/"Legends Club"; ABN 12 482 409 883 is published, the registered entity name is NOT (owed by Max Jones — never invent it); no phone/email exists yet.
>
> Current open items are in §5 of LL-HANDOFF.md. Start by running `npm ci && npm run api & npm run dev` then `node verify/smoke.mjs`.

---

## 2. What is built (all six milestones, 20 Sep 2026)

| Piece | State |
|---|---|
| Marketing site | 15 routes (home, book, services, pricing, legends-club, coverage, business, supported-living, operators, about, guarantee, faq, contact, privacy, terms) + NotFound. Copy extracted from the old single-file site with the Laundrylegends→Linen Legends rebrand pass. |
| Customer app | `/account` (Uber-style status timeline driven only by real `statusHistory`, explicit "not live GPS" line), `/messages` (derived from booking events, honest about no SMS/push). Booking refs `LL-XXXXXX` stored in localStorage `ll-bookings`. |
| API | POST `/api/estimate`, GET `/api/coverage/check`, POST `/api/waitlist`, POST+GET `/api/bookings`, POST `/api/enquiries` (3 kinds), POST `/api/contact`. Honeypot + in-memory rate limit. Persistence: `OPS_SHEET_WEBHOOK` env when set, else `./data` locally / `/tmp/ll-data` (EPHEMERAL) on Vercel. |
| Design system | tailwind.config.js tokens navy `#0C265F` / ember `#C43828` / ink/body/line/paper; Archivo + Inter self-hosted via @fontsource-variable; `.btn-primary/.btn-navy/.btn-outline/.input/.card/.eyebrow/.container-x`; Reveal/Kinetic/Marquee/Section primitives; Lenis (off for touch + reduced motion); glass shrink navbar, fullscreen overlay menu, scroll progress bar, magnetic CTA + club-card tilt (`src/lib/hooks.js`). |
| SEO | Per-route titles/canonicals via `src/components/Seo.jsx`; LocalBusiness/Service/FAQPage JSON-LD from `src/lib/jsonld.js` on `https://linenlegends.com.au`. |
| Gates | `node verify/smoke.mjs` (15 API cases: 77.50 / 73.50 / 109.50+48h / 4131 active / 4509 opening / 9000 waitlist / booking LL-ref / forms ok / bad-input rejects) · `node verify/sentinels.mjs` on `dist/` (9 must-have compliance strings, 8 must-nots) · `node verify/shots.mjs` (Playwright, all routes × 390/1280, zero console errors, menu toggle; uses reduced-motion emulation + scroll pass). |

## 3. Environment and identifiers

| Thing | Value |
|---|---|
| Vercel project | `linenlegends-site`, id `prj_4QSfN6wI0ixaBGqw5GD7DfeKifuG`, team `team_NUTrq3cCvN2Le5zQQJ9NTCGs` (slug keatonmills). Git-linked; every push deploys. Preview URLs sit behind Vercel deployment protection — view logged into Vercel. |
| Vercel production branch | Still the repo default (`claude/document-review-expand-u6pv21`, old content). Before launch either repoint the project's production branch to the build branch or merge. |
| Domain | `linenlegends.com.au` — REGISTERED 20 Sep 2026 (exact spelling confirmed). DNS not yet pointed at Vercel. |
| ABN | 12 482 409 883 (checksum-valid, supplied by Callum). Registered entity name UNVERIFIED/pending — Max Jones supplies it; footer says "Registered entity name to be published". |
| Local dev | Node 22. `npm ci` · `npm run api` (Express mount of the same handlers, :8787) · `npm run dev` (Vite :5173, proxies /api) · `npm run build` (runs the extract step first). Playwright Chromium at `/opt/pw-browsers/chromium-1194/.../chrome`. |
| Generated file | `src/data/site.js` is GENERATED by `tools/extract_site_data.mjs` (git-ignored) — edit the tool, never the output. `.gitignore` uses `/data/` root-scoped; do not widen it (a bare `data/` once swallowed `src/data/` and broke the Vercel build). |
| Canva (design copy only) | Home page imported into Canva Websites on 20 Sep 2026 as design `DAHVxin-gjU` ("Linen Legends — Home", edit: https://www.canva.com/d/n01_VJ5scljyNOn). Source is `export/canva/home.html`, a static pre-render made by `tools/snapshot_page.mjs` (Canva's importer reads static HTML/CSS only — no JS, one page per import). It is a visual copy for design work: the estimator, postcode check, booking and API do not run in Canva. The Vite/Vercel build stays the system of record; other routes need their own snapshot + import if wanted. Regenerate: run api+dev, then `node tools/snapshot_page.mjs export/canva/<name>.html /<route>`, push, import the raw GitHub URL. |

## 4. Decisions locked (do not relitigate)

D1 4000=opening (test with 4131) · D2 48 h is indicative, estimator-only · D3 pricing/privacy/terms are routes · D4 customer app in scope, built · D5 §4 of the plan is the backend spec · D7 superseded: brand "Linen Legends"/"Legends Club" · D9 no horizontal rail · D10 status timeline only, live GPS map is phase 2 · Rebrand + domain per plan §12.1–12.2. No fake testimonials ever; a real review system is a later phase seeded from the old `#/feedback` pattern.

## 5. Open items (the actual to-do list)

| # | Item | Owner |
|---|---|---|
| 1 | ~~Audit-and-fix workflow / polish pass~~ **CLOSED 20 Sep 2026** (`d8c1940`). The 43-file adversarial-audit workflow from the prior session was not resumed (working tree was clean at session start — nothing to recover). Instead shipped the three concrete polish items directly: hero parallax layer (`useParallax` in `src/lib/hooks.js`, applied to Home's decorative gradient), checkmark-draw success states (`CheckmarkDraw` + `SuccessCard` components, used in booking/enquiry/contact confirmations), and booking validation hardening (postcode/email/phone/window/date now format-checked server-side in `api/bookings.js` via `api/_lib/validate.js`, plus native `pattern` attrs client-side — patterns shared from `src/lib/bookingOptions.js` so client and server can't drift). Gates: smoke 15/15, build clean, sentinels 18/18, shots 17 routes ×2 widths × 0 console errors. If the full 43-file adversarial copy-audit (vs. PDF/old site) is still wanted, it hasn't run this session — re-run only if asked. | Closed |
| 2 | D6: Ops Sheet Apps Script webhook URL → set `OPS_SHEET_WEBHOOK` in Vercel env. Until then deployed bookings are ephemeral. | Callum |
| 3 | Registered entity name for the footer (goes with the ABN). | Max Jones |
| 4 | Repoint Vercel production branch / merge, then DNS `linenlegends.com.au` → Vercel; apex canonical, www redirect. | Callum + next session |
| 5 | Business blockers before public launch: phone/email or drop the promise; privacy+terms in force before real bookings (booking form carries the pre-launch registration-of-interest banner until then); pricing sign-off (Max). | Principals |
| 6 | Photography/film, final logo/typeface, analytics (Vercel Analytics recommended), operator portal rebuild, real review system — all deliberately LAST, per Callum. | Later |

## 6. Rules that travel (enforced by verify/sentinels.mjs — keep it that way)

GST-inclusive statement · ABN string · entity-name-pending string · Australian owned · NDIS non-registration statement · AS/NZS 4146 statement · ACL not-limited statement · contractor-not-employment statement · draft-policies "not yet in force" · brand "Linen Legends" · NEVER: old brands, competitor links/phone, invented phone numbers, emoji in our source.
